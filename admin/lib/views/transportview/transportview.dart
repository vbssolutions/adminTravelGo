import 'package:flutter/material.dart';

class TransportView extends StatelessWidget {
  static const String id = "transportview";
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
